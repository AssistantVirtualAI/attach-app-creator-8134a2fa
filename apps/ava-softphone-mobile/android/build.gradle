buildscript {
    repositories {
        google()
        mavenCentral()
        maven { url "https://jitpack.io" }
    }
    dependencies {
        classpath 'org.jetbrains.kotlin:kotlin-gradle-plugin:1.9.10'
        classpath 'com.android.tools.build:gradle:8.2.1'
        classpath 'com.google.gms:google-services:4.4.0'
    }
}

apply from: "variables.gradle"

allprojects {
    repositories {
        google()
        mavenCentral()
        maven { url "https://jitpack.io" }
    }
    // Force a newer Material version to avoid AGP 8.2.1 transforms-3 cache
    // corruption with material-1.10.0 (missing aar-metadata.properties).
    configurations.all {
        resolutionStrategy {
            force 'com.google.android.material:material:1.12.0'
        }
    }
}

subprojects {
    afterEvaluate {
        if (it.plugins.hasPlugin('com.android.library') || it.plugins.hasPlugin('com.android.application')) {
            android {
                compileOptions {
                    sourceCompatibility JavaVersion.VERSION_17
                    targetCompatibility JavaVersion.VERSION_17
                }
            }
        }
        tasks.matching { task ->
            task.class.name.contains('KotlinCompile')
        }.configureEach { task ->
            task.kotlinOptions.jvmTarget = "17"
        }
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
